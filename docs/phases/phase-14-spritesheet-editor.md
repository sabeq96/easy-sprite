In the spritesheet editor the dropzone should be full size. Now it's 480x480 fixed.
I want it to be zoomable with +, - control like in prite editor and also have zoom level shown.
I also want grid to be enabled by default so I can see if sprites snaps correctly.
The layout of spritesheet editor is odd. On smaller sizes the export button from header is not visible. I think layout is not responsive.
I want it to have snappy behaviour. All sprites should snap to the left when there is available space.
It should also sort sprites showing when dragging over (similar pattern we have with colors).
The grid size should be infinie but sprites should snap top left. It should not be all the way top, but rather next available row. There should not be a row gap between and no column gap between sprites.

Do grill-me session and prepare a plan for implementing. A plan should have list of files to be edited, and real implementation examples.
