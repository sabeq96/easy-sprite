// The base-nova shadcn style imports `cn` straight from the package; app code goes through
// here so there is a single import path that survives future `npx shadcn add` runs.
export { cn } from "cn";
